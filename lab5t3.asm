; Lab 5 task 3
DATA SEGMENT PARA PUBLIC 'DATA'

	NUM1	DB	8
	NUM2 	DB	9
	REZ		DB	?
	TMP		DB	?

DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
	START PROC FAR
		ASSUME CS:CODE, DS:DATA

		PUSH	DS
		XOR		AX, AX
		PUSH 	AX
		MOV 	AX, DATA
		MOV 	DS, AX
		
		MOV 	AX, 5H
		
		MOV		TMP, AL
		MUL		NUM1
		XCHG	TMP, AL
		MUL		NUM2
		ADD		AL, TMP
		ADD		AL, BL
		MOV		REZ, AL
		
		RET
	START ENDP
CODE ENDS
END START