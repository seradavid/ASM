; Lab 5 task 4
DATA SEGMENT PARA PUBLIC 'DATA'

	REZ		DB	?
	TMP		DW	?

DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
	START PROC FAR
		ASSUME CS:CODE, DS:DATA

		PUSH	DS
		XOR		AX, AX
		PUSH 	AX
		MOV 	AX, DATA
		MOV 	DS, AX
		
		MOV		AX, 9
		MOV 	BX, 10
		MOV 	TMP, AX
		
		SHL		AX, 3
		SUB		AX, TMP
		SHL		BX, 1
		SUB		AX, BX
		SHR		BX, 4
		SUB		AX, BX
		
		RET
	START ENDP
CODE ENDS
END START