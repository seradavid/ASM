; Lab 7 task 5
CODE SEGMENT PARA PUBLIC 'CODE'
	START PROC FAR
	ASSUME CS:CODE
	
	PUSH	DS
	XOR 	AX, AX
	PUSH 	AX
	
	MOV		AX, 1234
	
	MOV		BX, 10
	XOR 	CX, CX
	XOR		DX, DX
	
	NEXT_DIGIT:
		DIV		BX
		PUSH	DX
		INC		CX
		XOR		DX, DX
		CMP		AX, 0
		JNE		NEXT_DIGIT
	
	MOV		AH, 02H
	
	PRINT:
		POP		DX
		ADD		DL, 30H
		INT		21H
		LOOP 	PRINT
	
	RET
	START ENDP
CODE ENDS
END START